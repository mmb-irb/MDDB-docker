apiVersion: 1

# This file is used to configure the Prometheus datasource in Grafana. 
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
